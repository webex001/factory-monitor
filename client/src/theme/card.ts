/* Shared base for the bordered white "card" container used across KPI
   cards, skeletons, and panel wrappers on all three screens — compose with
   spacing/overflow/width modifiers per usage rather than repeating the base. */
export const CARD_CLASS = 'rounded-lg border border-slate-200 bg-white'
