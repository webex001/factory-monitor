{
  "name": "mock-server",
  "version": "1.0.0",
  "description": "Mock backend standing in for the DAQ -> processing -> PostgreSQL -> REST API pipeline. REST + SSE endpoints matching the real API's shape.",
  "private": true,
  "type": "commonjs",
  "main": "index.js",
  "scripts": {
    "start": "node index.js",
    "dev": "node --watch index.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  }
}
